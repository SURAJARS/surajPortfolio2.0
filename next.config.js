/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  images: {
    unoptimized: true,
  },
  basePath: "/surajPortfolio2.0",
  assetPrefix: "/surajPortfolio2.0",
};

module.exports = nextConfig;
